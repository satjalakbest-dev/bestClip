{
  "name": "bestclip-remotion",
  "version": "1.0.0",
  "description": "BlueOclock-style video renderer with Remotion",
  "scripts": {
    "start": "cd remotion && npx remotion studio",
    "render": "cd remotion && npx remotion render BlueOclockVideo",
    "preview": "cd remotion && npx remotion studio --props='../output/script.json'"
  },
  "dependencies": {
    "remotion": "^4.0.0",
    "@remotion/cli": "^4.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "typescript": "^5.0.0"
  }
}
